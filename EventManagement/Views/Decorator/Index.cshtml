@model IEnumerable<EventManagement.Models.Decorator>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DecoratorsName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Charges)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DecoratorsName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Charges)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DecoratorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DecoratorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DecoratorID })
        </td>
    </tr>
}

</table>
